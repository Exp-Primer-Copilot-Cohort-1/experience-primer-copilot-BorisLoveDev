// Create web server
const express = require('express')
const app = express()

// Create a route for GET /comments
app.get('/comments', (req, res) => {
  res.json({ comments: [] })
})

// Start the server
app.listen(3000, () => {
  console.log('Server is running on http://localhost:3000')
})
```

To run the web server, use the following command:

```sh
node comments.js
```

Now, you can access the server on `http://localhost:3000/comments`.

### Create a web server with Express

Create a new file `comments.js` and add the following code:

```javascript
// Path: comments.js
// Create web server
const express = require('express')
const app = express()

// Create a route for GET /comments
app.get('/comments', (req, res) => {
  res.json({ comments: [] })
})

// Start the server
app.listen(3000, () => {
  console.log('Server is running on http://localhost:3000')
})
```

To run the web server, use the following command:

```sh
node comments.js
```

Now, you can access the server on `http://localhost:3000/comments`.

### Create a web server with Express

Create a new file `comments.js` and add the following code:

```javascript
// Path: comments.js
// Create web server
const express = require('express')
const app = express()

// Create a route for GET /comments
app.get('/comments', (req, res) => {
  res.json({ comments: [] })
})

// Start the server
app.listen(3000, () => {
  console.log('Server is running on http://localhost:3000')
})
```

To run the web server, use the following command:

```sh
node comments.js
```

Now, you can access the server on `http://localhost:3000/comments`.

### Create a web server with Express

Create a new file `comments.js` and add the following code:

```javascript
// Path: comments.js
// Create web server
const express = require('express')
const app = express()

// Create a route for GET /comments
app.get('/comments', (req, res) => {
  res.json({ comments: [] })
})

// Start the server
app.listen(3000, () => {
